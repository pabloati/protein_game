# protein_game
Repository with the scripts and database to generate the streamlit app to find the protein encoded within your name

In order to run the streamlit app, you need to have conda. Create the conda environment from the file and use pip to install the dependencies in the requirements.txt file:

```bash
conda create -n protein_game -f conda_environment.yaml
conda activate protein_game
pip install -r requirements.txt
```